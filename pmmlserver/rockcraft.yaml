# Based on https://github.com/kserve/kserve/blob/v0.16.0/python/pmml.Dockerfile
# 
# See ../CONTRIBUTING.md for more details about the patterns used in this rock. 
# This rock is implemented with some atypical patterns due to the nature of the upstream
# Dockerfile.
name: pmmlserver
summary: Pmml server for Kserve deployments
description: "Kserve Pmml server"
version: "0.16.0"
license: Apache-2.0
base: ubuntu@22.04
platforms:
    amd64:
run-user: _daemon_
services:
  pmmlserver:
    override: replace
    summary: "Pmml server service"
    startup: enabled
    command: "python -m pmmlserver [ dummy-arguments ]"
    environment:
      PYTHONPATH: /pmmlserver
entrypoint-service: pmmlserver

parts:
  security-team-requirement:
    plugin: nil
    override-build: |
      mkdir -p ${CRAFT_PART_INSTALL}/usr/share/rocks
      (echo "# os-release" && cat /etc/os-release && echo "# dpkg-query" && \
      dpkg-query --root=${CRAFT_PROJECT_DIR}/../bundles/ubuntu-22.04/rootfs/ -f '${db:Status-Abbrev},${binary:Package},${Version},${source:Package},${Source:Version}\n' -W) \
      > ${CRAFT_PART_INSTALL}/usr/share/rocks/dpkg.query

  python:
    plugin: nil
    source: https://github.com/kserve/kserve.git
    source-subdir: python
    source-tag: v0.16.0
    build-packages:
    - python3.11
    - python3.11-venv
    - python3-pip
    - openjdk-21-jdk
    - curl
    - gcc
    - build-essential
    overlay-packages:
    - python3.11
    - openjdk-21-jdk
    override-build: |
      # Ensure Python 3.11 is the default version
      update-alternatives --install /usr/bin/python3 python3 /usr/bin/python3.11 1
      update-alternatives --set python3 /usr/bin/python3.11

      # Install uv
      curl -LsSf https://astral.sh/uv/install.sh | sh
      export PATH="/root/.local/bin:$PATH"

      # Setup virtual environment
      uv venv ./python_venv
      export VIRTUAL_ENV=$PWD/python_venv
      export PATH="$VIRTUAL_ENV/bin:$PATH"

      # Install the kserve, storage, and pmmlserver packages with their dependencies using uv
      # Install kserve
      cd python/kserve && uv sync --active --no-cache && cd ../..
      
      # Install storage  
      cd python/storage && uv sync --active --no-cache && cd ../..
      cd python/storage && uv pip install . --no-cache && cd ../..
      
      # Install pmmlserver - use uv sync for dependencies, then pip install for the package
      cd python/pmmlserver && uv sync --active --no-cache && cd ../..
      cd python/pmmlserver && uv pip install . --no-cache && cd ../..

      # Promote the packages we've installed from the virtual env to the primed image
      mkdir -p $CRAFT_PART_INSTALL/usr/local/lib/python3.11/dist-packages
      cp -fr ./python_venv/lib/python3.11/site-packages/* $CRAFT_PART_INSTALL/usr/local/lib/python3.11/dist-packages/

      # Ensure `python` is an executable command in our primed image
      mkdir -p $CRAFT_PART_INSTALL/usr/bin/
      ln -s /usr/bin/python3.11 $CRAFT_PART_INSTALL/usr/bin/python

      # Move Py4J jar file to $CRAFT_PART_INSTALL
      mkdir -p $CRAFT_PART_INSTALL/usr/local/share
      cp -fr /usr/local/share/py4j $CRAFT_PART_INSTALL/usr/local/share/ || true

      # Third party licenses
      cd python
      cp third_party/pip-licenses.py pip-licenses.py
      uv pip install --no-cache-dir tomli requests
      mkdir -p third_party/library && python3 pip-licenses.py
      mkdir -p ${CRAFT_PART_INSTALL}/third_party
      cp -fr third_party/* ${CRAFT_PART_INSTALL}/third_party
